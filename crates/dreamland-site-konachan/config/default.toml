# Konachan's real site and API are hosted on .com. Requests may still be
# challenged by bot protection, but the adapter must not silently replace the
# real site with the safe-only .net mirror.
api_url = "https://konachan.com/post.json"
browser_url = "https://konachan.com/post"
